/**
 * Metadata command index - Exports the metadata command
 */

import { createMetadataCommand } from './metadata-command.ts';

/**
 * Create and export the metadata command
 */
export { createMetadataCommand };