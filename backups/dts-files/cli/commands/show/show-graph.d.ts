/**
 * Show graph command implementation
 * Displays the task hierarchy with improved visualization
 */
import { Command } from 'commander';
export declare function createShowGraphCommand(): Promise<Command>;
