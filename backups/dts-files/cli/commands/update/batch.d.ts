/**
 * Batch update command implementation
 * Handles updating multiple tasks in a single operation via JSON file
 */
import { Command } from 'commander';
export declare function createBatchUpdateCommand(): Promise<Command>;
