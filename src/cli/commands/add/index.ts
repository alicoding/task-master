/**
 * Add command - Exports the CLI command for adding tasks
 */

import { createAddCommand } from '@/cli/commands/add/add-command';

/**
 * Create and export the add command for CLI
 */
export { createAddCommand };